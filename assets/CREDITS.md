# Asset credits and licenses

Art direction is hand-painted / illustrated HD 2D, authored at 1920×1080.

Add a row here **as you add each source** — commissioned work, licensed pack, or
audio. You need commercial-use rights for anything that ships on Steam, and
retrofitting this after a hundred assets is miserable.

| Asset | Author | Source | License / contract | Attribution required? | Used for |
|---|---|---|---|---|---|
| `characters/player_spritesheet_3.png` | **unrecorded** | **unrecorded** | **unrecorded** | **unknown** | Player animation — idle, run, jump, slide, crouch, crouch-walk and three firing poses |
| `characters/player_spritesheet_2.png` | **unrecorded** | **unrecorded** | **unrecorded** | **unknown** | Superseded second pass; not referenced by any scene |
| `characters/player_spriteheet.png` | **unrecorded** | **unrecorded** | **unrecorded** | **unknown** | Superseded first pass; not referenced by any scene |
| `backgrounds/background_reference.png` | **unrecorded** | **unrecorded** | **unrecorded** | **unknown** | Style/palette reference only — not shipped, but see the note below |
| `backgrounds/generated/*.png` | generated by `tools/gen_background.py` | this repo | project-owned | no | Parallax background layers |

> **Second open question.** `background_reference.png` was supplied as a style
> reference. The generated layers take their **palette and composition
> direction** from it — sampled colours, horizon placement, layer count — rather
> than reproducing its artwork, and no pixel of it ships. That is normally fine,
> but if the reference is a third party's work, record whose before the
> generated layers get replaced by anything traced or closely derived from it.
> A mood collage or an original sketch removes the question entirely.

> **Open question — needs answering before this ships.** All three sheets were
> added to the repo without provenance. If they were commissioned, the contract
> needs to be checked against the list below and the rows filled in. If they were
> generated or downloaded, that needs recording too, because it decides whether
> the game can ship on Steam with them. `player_spritesheet_3.png` is the one
> currently driving the player, so it is the one that matters.

## Checklist before adding anything

- [ ] Commercial use permitted
- [ ] Modification permitted (you will recolor and edit)
- [ ] Resolution and painting style match what's already in the project
- [ ] Attribution text copied here verbatim if required
- [ ] Source files (layered PSD/Krita, not just flattened PNGs) archived outside the repo

## For commissioned work specifically

- [ ] Contract explicitly assigns **full commercial rights**, not a limited license
- [ ] Deliverables include layered source files and sprite sheets, not just exports
- [ ] Animation timings agreed against the locked movement values from
      `scenes/player/player.gd` — supply `jump_time_to_peak`, `jump_time_to_fall`,
      and `run_speed` as part of the brief
- [ ] Canvas size / character height in pixels specified up front (player ≈ 170px tall)
- [ ] For **background layers**, each one specified as either a minimum pixel
      width or as seamlessly tileable on x — the two are different deliverables
      and neither can be retrofitted cheaply. The widths come from the formula in
      the README's Backgrounds section; `desert_bg.tscn` carries the current
      per-layer numbers in comments.
